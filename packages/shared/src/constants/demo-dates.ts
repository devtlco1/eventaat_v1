/**
 * Mock “calendar day” aligned with sample reservation datetimes in mock data
 * (web restaurant dashboard uses the same for “اليوم” / late logic).
 */
export const MOCK_DEMO_TODAY = '2026-04-27' as const;
