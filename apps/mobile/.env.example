# API base for real auth/OTP (Phase 2D). On a physical device use your computer LAN IP, not localhost.
# Example: EXPO_PUBLIC_API_BASE_URL=http://192.168.1.12:3000
EXPO_PUBLIC_API_BASE_URL=

# If true, keep the mock-only auth path even when a base URL is set.
# EXPO_PUBLIC_MOCK_AUTH=false
