/**
 * Type-only re-exports from generated Prisma client for auth services.
 */
import type { User, UserRoleAssignment, OtpChallenge, UserSession, AuditLog } from '../../generated/client';

export type { User, UserRoleAssignment, OtpChallenge, UserSession, AuditLog };
